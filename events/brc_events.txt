namespace = better_regime_change

better_regime_change.1 = {
	hidden = yes
	immediate = {
		every_interest_group_in_government = {
			remove_ruling_interest_group = yes
		}
		if = {
			limit = {
				scope:leader_ig = {
					has_party = yes
				}
			 }
			 scope:leader_ig.party = {
				set_ruling_party = yes
			 }
		} else = {
			scope:leader_ig = {
				add_ruling_interest_group = yes
			}
		}
		scope:leader_ig = {
			leader = {
				set_character_as_ruler = yes
			}
		}
	}
}