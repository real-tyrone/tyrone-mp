namespace = legitimist_events

legitimist_events.1 = {
    type = country_event
    placement = root

    title = legitimist_events.1.t
    desc = legitimist_events.1.d
    flavor = legitimist_events.1.f

    event_image = {
        video = "votp_monarch_holding_court"
    }

    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

    icon = "gfx/interface/icons/event_icons/house_of_bourbon.dds"

    duration = 3


    option = {
        name = legitimist_events.1.a
        default_option = yes

		add_journal_entry = {
            type = je_house_of_bourbon
        }
    }
}

legitimist_events.5 = {
    type = country_event
    placement = root

    title = legitimist_events.5.t
    desc = legitimist_events.5.d
    flavor = legitimist_events.5.f

    event_image = {
        video = "votp_monarch_holding_court"
    }

    on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

    icon = "gfx/interface/icons/event_icons/house_of_bourbon.dds"

    duration = 3


    option = {
        name = legitimist_events.5.a
        default_option = yes
		add_loyalists = {
            pop_type = aristocrats
            value = large_radicals
        }
        add_loyalists = {
            pop_type = clergymen
            value = large_radicals
        }
        ruler = {
            add_modifier = legitimist_ruler_modifier 
        }
    }
}
