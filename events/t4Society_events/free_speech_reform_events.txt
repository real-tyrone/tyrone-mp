namespace free_speech_reform

free_speech_reform.1 = {
	type = country_event

	event_image = {
		video = "unspecific_world_fair"
	}

	on_created_soundeffect = "event:/SFX/UI/Alerts/event_appear"

	icon = "gfx/interface/icons/event_icons/event_newspaper.dds"

	duration = 3

	cooldown = { days = never_fire_again_modifier_time }

	title = free_speech_reform.1.t
	desc = free_speech_reform.1.d
	flavor = free_speech_reform.1.f

	trigger = {
	}

	immediate = {
	}

	option = {
		name = free_speech_reform.1.a
		default_option = yes
		set_variable = { which = propaganda_leader_choice value = 1 }
		set_variable = intelligentsia_speechreform_on
		remove_variable = industrialist_speechreform_on
		remove_variable = armed_forces_speechreform_on
	}
	option = {
		name = free_speech_reform.1.b
		set_variable = { which = propaganda_leader_choice value = 2 }
		trigger = {
			has_law_or_variant = law_type:law_outlawed_dissent
		}
	}
	option = {
		name = free_speech_reform.1.c
		set_variable = { which = propaganda_leader_choice value = 3 }
		remove_variable = intelligentsia_speechreform_on
		remove_variable = industrialist_speechreform_on
		set_variable = armed_forces_speechreform_on
		trigger = {
			has_law_or_variant = law_type:law_outlawed_dissent
		}
	}
	option = {
		name = free_speech_reform.1.e
		set_variable = { which = propaganda_leader_choice value = 4 }
		remove_variable = intelligentsia_speechreform_on
		set_variable = industrialist_speechreform_on
		remove_variable = armed_forces_speechreform_on
		trigger = {
			has_law_or_variant = law_type:law_outlawed_dissent
		}
	}
	option = {
		name = free_speech_reform.1.g
		set_variable = { which = propaganda_leader_choice value = 1 }
		set_variable = intelligentsia_speechreform_on
		remove_variable = industrialist_speechreform_on
		remove_variable = armed_forces_speechreform_on
		trigger = {
			has_law_or_variant = law_type:law_protected_speech
		}
	}
	option = {
		name = free_speech_reform.1.h
		set_variable = { which = propaganda_leader_choice value = 5 }
		trigger = {
			has_law_or_variant = law_type:law_protected_speech
		}
	}
	option = {
		name = free_speech_reform.1.i
		set_variable = { which = propaganda_leader_choice value = 6 }
		trigger = {
			has_law_or_variant = law_type:law_protected_speech
		}
	}

}
